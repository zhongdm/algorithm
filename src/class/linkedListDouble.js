/**
 * 双向链表
 */

 class Node {
   constructor (element) {
     this.prev = null
     this.element = element
     this.next = null
   }
 }

 class LinkedListDouble {
   constructor() {
     this.head = null
     this.length = 0
   }

   /**
    * 添加元素
    * @param {*} element 
    */
   append (element) {

   }

   /**
    * 插入元素
    * @param {*} position 
    * @param {*} element 
    */
   insert (position, element)
 }